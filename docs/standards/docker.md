# Docker Standards

This document outlines Docker usage expectations for the project.

## Guidelines

- Keep container images small and reproducible
- Use environment variables for configuration
- Prefer explicit startup and health-check behavior
- Avoid embedding secrets in image layers

## Notes

Update this file when container build and runtime practices change.
