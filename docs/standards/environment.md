# Environment Standards

This document defines how environment configuration should be handled.

## Guidelines

- Keep configuration in environment variables
- Validate required values at startup
- Keep development, test, staging, and production values separate
- Never commit secrets

## Notes

Use this document alongside the configuration platform guidance in the repository.
