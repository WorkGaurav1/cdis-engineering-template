## What changed and why

<!-- One or two sentences. Focus on why, not just what — the diff already shows what. -->

## Checklist

- [ ] Ran the relevant checks locally before pushing (`npm run lint && npx tsc -b && npm test` for the app(s) touched)
- [ ] Added/updated tests for the change
- [ ] Updated the matching doc in `docs/` if this changes a workflow, an architecture decision, or a security control
- [ ] If this touches `front-end/`, `back-end/`, or `deployment/`, ran `scripts/export-to-repos.sh` and reviewed the resulting diff in the sibling repo(s)

## How to verify

<!-- Exact commands/steps a reviewer can run to confirm this works. -->
